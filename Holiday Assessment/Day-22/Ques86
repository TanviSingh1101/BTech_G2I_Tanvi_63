#include<iostream>
#include<cstring>
using namespace std;
int main()
{
    int n,i,word=0;
    char str[100],rst[100];
    cout<<"Enter the string";
    cin.getline(str,100);
    n=strlen(str);
   for(i=0;i<n;i++)
   {
        if(str[i]==' ')
        word++;
   }
   cout<<"The no of words are "<<word+1;
    return 0;
}
